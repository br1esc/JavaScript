'use strict'

const topics = ['JavaScript', 'Variables', 'funciones', 'condicionales', 'bucles'];
const scipot = topics.reverse ( );
const SCIPOT = scipot.map(eachTopic =>eachTopic.toUpperCase());


console.log(SCIPOT);